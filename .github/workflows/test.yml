name: test

on:
  push:
  pull_request:

jobs:
  lilypond:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install LilyPond
        run: sudo apt-get update && sudo apt-get install -y lilypond
      - name: Compile every movie example
        run: make check
